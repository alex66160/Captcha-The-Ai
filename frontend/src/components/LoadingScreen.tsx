/**
 * The LoadingScreen component is just a loading screen that shows up when the lobbyState is in the process of being fetched.
 * @author Alex Liu
 */
function LoadingScreen() {
    return <p> LOADING... </p>;
}

export default LoadingScreen;
